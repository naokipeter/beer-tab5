.arduino/
build/
.DS_Store
secrets.h
wifi_secrets.h
*.local.*
# Editor swap, backup and undo files. Any of them taken from secrets.h holds
# the secrets in clear. Note that .gitignore does not untrack a file that is
# already in the index, which is how secrets.h~ reached this history.
*.swp
*.swo
*~
*.un~
.*.sw?
