#pragma once
// Copy to secrets.h for milestone 7. Never add real values to this example.
// Wi-Fi credentials will be provisioned separately, outside tracked files.
namespace secrets {
inline constexpr char endpoint_url[] = "https://script.google.com/macros/s/REPLACE/exec";
inline constexpr char device_token[] = "REPLACE";
}
